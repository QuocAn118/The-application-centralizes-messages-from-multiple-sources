# Cơ sở dữ liệu — PostgreSQL 17 cài native trên Windows
DATABASE_URL=postgresql+psycopg://postgres@localhost:5432/omnichat
TEST_DATABASE_URL=postgresql+psycopg://postgres@localhost:5432/omnichat_test

# JWT — sinh khoá bằng: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=thay-bang-khoa-that-truoc-khi-chay
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Rate limit cho endpoint dang nhap
LOGIN_RATE_LIMIT_ATTEMPTS=5
LOGIN_RATE_LIMIT_WINDOW_SECONDS=300

# Inbox — mã hoá credential kênh (Zalo/Meta token). Sinh khoá bằng:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CHANNEL_CIPHER_KEY=thay-bang-khoa-fernet-that-truoc-khi-chay
# Thư mục lưu tệp đính kèm tải về (dev: đĩa local, đã gitignore)
ATTACHMENT_STORAGE_DIR=var/attachments

# Inbox — bí mật cấp ứng dụng để verify chữ ký webhook (KHÔNG phải token kênh).
# Zalo: app id + OA secret key (mục Webhook trong Zalo OA).
ZALO_APP_ID=
ZALO_OA_SECRET_KEY=
# Meta: app secret (Settings > Basic của app Meta), dùng chung FB + IG.
META_APP_SECRET=
# Token tự đặt để Meta/Zalo verify webhook (GET hub.challenge); rỗng = bỏ qua.
WEBHOOK_VERIFY_TOKEN=

# Telegram Bot API (kenh thu ba cua #1, dung cho test/demo noi bo).
# Token lay tu @BotFather bang lenh /newbot, dang <bot_id>:<chuoi>.
TELEGRAM_BOT_TOKEN=
# Secret TU DAT, truyen vao setWebhook; Telegram gui lai o moi request.
# Sinh bang: python -c "import secrets; print(secrets.token_urlsafe(32))"
# RONG = TU CHOI MOI webhook Telegram (khac WEBHOOK_VERIFY_TOKEN: rong = bo qua).
TELEGRAM_WEBHOOK_SECRET=

# Dia chi CONG KHAI cua API, de Zalo/Meta tai anh gui kem ve. Bat buoc neu muon
# gui anh ra nen tang: khong suy ra duoc tu request (server thuong sau NAT/proxy).
# Rong = van luu anh vao lich su nhung KHONG gui kem ra nen tang.
# Vi du khi dev: dung ngrok/cloudflared roi dat URL cong khai vao day.
ATTACHMENT_PUBLIC_BASE_URL=
# Tran kich thuoc mot tep gui kem (byte). Mac dinh 10MB.
ATTACHMENT_MAX_BYTES=10485760
# So giay mot lien ket anh dinh kem con hieu luc (link ky so, tu het han).
ATTACHMENT_URL_TTL_SECONDS=300

# CORS: origin cua frontend duoc phep goi API tu trinh duyet (ngan cach bang dau
# phay). Frontend chay o cong khac BAT BUOC co mat o day, neu khong trinh duyet
# chan tu buoc preflight. Khong dung "*" vi API dung Authorization + cookie.
CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Keyword (#2): khoa Claude API de LLM doc tin va chon phong phu hop.
# RONG = KHONG phan tich: moi hoi thoai dung o CHO_PHAN va #3 khong tu gan duoc.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5

# Mui gio nghiep vu: gio ca lam (#4) va so sanh "dang trong ca" (#3) theo gio
# dia phuong nay, du he thong luu moi moc thoi gian o UTC.
APP_TIMEZONE=Asia/Ho_Chi_Minh

# Ung dung
APP_ENV=development
LOG_LEVEL=INFO
